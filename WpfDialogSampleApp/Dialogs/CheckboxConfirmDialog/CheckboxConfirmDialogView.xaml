<UserControl x:Class="WpfDialogSampleApp.Dialogs.CheckboxConfirmDialog.CheckboxConfirmDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title.Value}" 
                   FontSize="14" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15" 
                   HorizontalAlignment="Center"/>

        <!-- メッセージ -->
        <TextBlock Grid.Row="1" 
                   Text="{Binding Message.Value}" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,20" 
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"/>

        <!-- チェックボックス -->
        <CheckBox Grid.Row="2"
                  Content="{Binding CheckboxText.Value}"
                  IsChecked="{Binding IsCheckboxChecked.Value}"
                  Margin="0,0,0,20"
                  VerticalAlignment="Center"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button Content="{Binding OkButtonText.Value}" 
                    Command="{Binding OkCommand}" 
                    IsDefault="True"
                    MinWidth="75" 
                    Margin="0,0,10,0" 
                    Padding="10,5"/>
            <Button Content="{Binding CancelButtonText.Value}" 
                    Command="{Binding CancelCommand}" 
                    IsCancel="True"
                    MinWidth="75" 
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
