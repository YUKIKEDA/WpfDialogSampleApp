<UserControl x:Class="WpfDialogSampleApp.Dialogs.ProgressDialog.ProgressDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title.Value}" 
                   FontSize="14" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15" 
                   HorizontalAlignment="Center"/>

        <!-- メッセージ -->
        <TextBlock Grid.Row="1" 
                   Text="{Binding Message.Value}" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,15" 
                   HorizontalAlignment="Left"/>

        <!-- 現在の操作 -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding CurrentOperation.Value}" 
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,0,0,10" 
                   HorizontalAlignment="Left"/>

        <!-- プログレスバー -->
        <ProgressBar Grid.Row="3"
                     Value="{Binding ProgressValue.Value}"
                     IsIndeterminate="{Binding IsIndeterminate.Value}"
                     Height="20"
                     Margin="0,0,0,10"/>

        <!-- 進捗パーセンテージ -->
        <TextBlock Grid.Row="4"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Text" Value="{Binding ProgressValue.Value, StringFormat={}{0:F1}%}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsIndeterminate.Value}" Value="True">
                            <Setter Property="Text" Value="処理中..."/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- キャンセルボタン -->
        <Button Grid.Row="6" 
                Content="{Binding CancelButtonText.Value}" 
                Command="{Binding CancelCommand}" 
                IsCancel="True"
                MinWidth="75" 
                Padding="10,5"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"/>
    </Grid>
</UserControl>
